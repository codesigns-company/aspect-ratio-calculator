name: Deploy Jekyll site to Pages

on:
  push:
    branches: ["master"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Setup Ruby
      uses: ruby/setup-ruby@v1
      with:
        ruby-version: '3.2'
        bundler: '2.4.22'

    - name: Install Ruby dependencies
      run: bundle install

    - name: Setup Node
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Build CSS
      run: |
        npm run build
        echo "CSS file size: $(wc -c < src/assets/css/main.css) bytes"
        ls -la src/assets/css/

    - name: Build with Jekyll
      run: |
        echo "Jekyll version: $(bundle exec jekyll --version)"
        echo "Ruby version: $(ruby --version)"
        bundle exec jekyll build --trace --verbose
        echo "Contents of web/assets after Jekyll build:"
        ls -la web/assets/ || echo "web/assets does not exist"
      env:
        JEKYLL_ENV: production

    - name: Ensure assets are copied
      run: |
        mkdir -p web/assets
        cp -r src/assets/* web/assets/ || echo "Failed to copy assets"
        echo "Final contents of web/assets:"
        ls -la web/assets/

    - name: Upload artifact
      uses: actions/upload-pages-artifact@v3
      with:
        path: ./web

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4